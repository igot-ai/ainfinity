name: {{ job_name }}

resources:
  accelerators: {{ accelerator_type }}
  cloud: gcp
  ports:
    - {{ port }}

setup: |
  pip install vllm

run: |
  python -m vllm.entrypoints.openai.api_server \
    --model {{ model_path }} \
    --port {{ port }} \
    --host 0.0.0.0
