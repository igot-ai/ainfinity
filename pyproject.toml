[project]
name = "llm-factory"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pre-commit>=4.5.0",
    "skypilot>=0.10.5",
    "transformers>=4.57.1",
]
